#
# Fichier : config/parameters.yml — bastien59960/reactions/config/parameters.yml
#

parameters:
    # =============================================================================
    # TABLES DE BASE DE DONNÉES
    # =============================================================================

    # Table principale des réactions
    tables.post_reactions: '%core.table_prefix%post_reactions'

    # Table des préférences utilisateur pour les notifications de réactions
    tables.reactions_user_prefs: '%core.table_prefix%reactions_user_prefs'

    # Table des sujets (référence)
    tables.topics: '%core.table_prefix%topics'

    # Table des forums (référence)
    tables.forums: '%core.table_prefix%forums'
